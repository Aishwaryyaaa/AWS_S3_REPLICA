pipeline {
    agent {
        label 'maven'
          }
    stages {
        stage ('gitpull'){
            steps {
                git 'https://github.com/Aishwaryyaaa/student-ui.git'
            }
        }
        stage ('install maven'){
            steps {
                sh 'sudo apt update'
                sh 'sudo apt-get install maven -y'
            }
        }
    }
    }
